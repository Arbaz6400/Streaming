plugins {
    id 'java'
    id 'maven-publish'
}

group = 'org.enbd'
version = '0.0.1'  // This is the version your pipeline reads

repositories {
    mavenCentral()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
