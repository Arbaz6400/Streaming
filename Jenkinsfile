pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the same repo where Jenkinsfile & groovies are stored
                checkout scm
            }
        }

   
    }
}
